# Domain docs

采用 single-context：根目录 `CONTEXT.md` 为领域词汇表，`docs/adr/` 存放架构决策。

探索项目或讨论设计前，阅读 `CONTEXT.md` 及与当前主题相关的 ADR。
文件不存在时直接继续，由 domain-modeling 在术语确定或确有决策需要记录时创建。

命名领域概念时遵循词汇表。发现术语冲突时明确指出并澄清。
CONTEXT.md 仅记录领域术语，不承载规格或实现细节。
方案与已有 ADR 冲突时，明确指出所冲突的决策及重新讨论的原因。
