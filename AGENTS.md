## Agent skills

### Issue tracker

事项存放于本地 `.scratch/`。创建、读取或更新事项前，阅读 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认分诊标签。分诊时阅读 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局。探索项目或讨论设计前，按 `docs/agents/domain.md` 阅读领域文档。
